filename = "kafka_input.lua"                              
brokerlist = "{kafka_host}:{kafka_port}"
topics = {"metering.sample"}
consumer_conf = {
    ["group.id"] = "metric_collector",
    ["message.max.bytes"] = 16024,
}
topic_conf = {}
decoder_module = "ceilometer"
metadata_fields = "{metadata_fields separated by space}"
ticker_interval = 60
